"""Multi-MCP configuration package.

This package contains bundled configuration files:
- config.yaml: Package defaults (models + settings)

User overrides go in ~/.multi_mcp/config.yaml
API keys go in .env files (project root or ~/.multi_mcp/.env)
"""
